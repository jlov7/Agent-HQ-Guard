{
  "name": "@agent-hq-guard/cli",
  "version": "0.1.0",
  "private": true,
  "bin": {
    "hqguard": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts --tsconfig tsconfig.json",
    "lint": "eslint --max-warnings=0 \"src/**/*.ts\"",
    "test": "vitest run --coverage",
    "typecheck": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@agent-hq-guard/policy": "workspace:*",
    "@agent-hq-guard/provenance": "workspace:*",
    "commander": "^14.0.2",
    "glob": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/glob": "^9.0.0",
    "eslint": "^9.39.1",
    "eslint-config-prettier": "^10.1.8",
    "tsup": "^7.2.0",
    "typescript": "^5.4.2",
    "vitest": "^4.0.10"
  }
}
